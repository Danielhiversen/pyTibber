sudo: false
matrix:
  fast_finish: true
  include:
  - python: '3.5'
    env: TOXENV=lint
cache:
  directories:
  - "$HOME/.cache/pip"
install:
- pip install tox flake8 pylint gql
language: python
script:
- flake8 Tibber --max-line-length=120
- pylint Tibber --max-line-length=120

